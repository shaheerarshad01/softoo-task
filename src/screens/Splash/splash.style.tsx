import {StyleSheet, Platform} from 'react-native';
import metrics from '../../utils/mertrics';

export default StyleSheet.create({
  container: {
    height: metrics.images.fullScreenHeight,
    width: metrics.images.fullScreenWidth,
  },
});
